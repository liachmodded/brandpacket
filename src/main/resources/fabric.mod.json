{
  "schemaVersion": 1,
  "id": "${id}",
  "version": "${version}",

  "name": "brandpacket",
  "description": "${description}",
  "authors": [
    "liach"
  ],
  "contact": {
    "homepage": "https://github.com/${repoOwner}/${repoName}",
    "sources": "https://github.com/${repoOwner}/${repoName}"
  },

  "license": "MPL-2.0",

  "environment": "client",
  "entrypoints": {
    "main": [
      "com.github.liachmodded.brandpacket.BrandPacket"
    ]
  },
  "mixins": [
    "brandpacket.mixins.json"
  ],

  "depends": {
    "fabricloader": "${loaderVersionLimit}",
    "minecraft": "${minecraftVersionLimit}"
  }
}
